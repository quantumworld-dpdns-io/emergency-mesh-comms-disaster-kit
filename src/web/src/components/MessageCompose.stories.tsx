import type { Meta, StoryObj } from "@storybook/react";
import MessageCompose from "./MessageCompose";

const meta: Meta<typeof MessageCompose> = { title: "Mesh/MessageCompose", component: MessageCompose };
export default meta;
export const Default: StoryObj<typeof MessageCompose> = {};

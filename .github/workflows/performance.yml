name: Performance
on: [workflow_dispatch]
jobs:
  locust:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Locust headless 50 users 5min placeholder with p95 assertion"
